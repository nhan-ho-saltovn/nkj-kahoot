import "./Timer.style.scss";

const Timer = () => {
  return (
    <div className="timer" style={{ width: 0 }}>
      <div className="timer-progress-bars"></div>
    </div>
  );
};

export default Timer;
